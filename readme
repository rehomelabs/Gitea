Here are the list of commands used:
cd /etc/komodo/stacks/gitea/gitea/gitea 
sudo mkdir -p options/gitignore
cd /options/gitignore
nano ENV 
# Add below to the ENv file adn save
*.env
docker run --entrypoint="" --rm -it docker.io/gitea/act_runner:latest act_runner generate-config > config.yaml
sudo mv config.yaml /etc/komodo/stacks/gitea/gitea/gitea/
